#!/usr/bin/env python
# coding: utf-8
"""Unit tests for the imagizer package.

Run the whole suite with::

    python3 -m imagizer.test

The tests are self-contained: they load the modules under test by file path
with minimal stubs, so they need neither a configured ~/.imagizer nor a running
Qt/graphical environment.
"""
